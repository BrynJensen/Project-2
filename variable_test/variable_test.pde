//animation lesson

void setup() {
   size(300, 300);
}

void draw() {
  background(mouseX, mouseX, mouseY);
  strokeWeight(mouseX);
  //ellipse (150, 150, 200, 200);
  line(300, 300, mouseX, mouseY);
}
